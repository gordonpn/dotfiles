# Personal Mac. Everything shared lives in ~/.gitconfig-common (symlink to
# dotfiles/dotfiles/.gitconfig-common) so the three machines cannot drift apart.
# Only machine-specific settings belong below.

[include]
path = ~/.gitconfig-common

[user]
name = Gordon Pham-Nguyen
email = gordon.pn6@gmail.com

[credential]
helper = osxkeychain
